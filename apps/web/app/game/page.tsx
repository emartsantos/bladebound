import { SectionContent } from '@/components/section-content';

/**
 * /game — the Activities hub is the default game section (gameplay hub).
 */
export default function GameHomePage() {
  return <SectionContent section="activities" />;
}